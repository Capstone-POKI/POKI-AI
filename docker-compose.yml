services:
  fastapi-ai:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./credentials:/app/credentials:ro
    restart: unless-stopped
    networks:
      - poki-net

networks:
  poki-net:
    external: true
